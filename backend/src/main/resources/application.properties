server.port=8080
spring.application.name=Aravindhan Portfolio

# Thymeleaf configuration
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# DevTools
spring.devtools.restart.enabled=true

# PostgreSQL Database Configuration (Using Environment Variables for Production)
spring.datasource.url=${DB_URL:jdbc:postgresql://aws-0-ap-northeast-1.pooler.supabase.com:6543/postgres?prepareThreshold=0}
spring.datasource.username=${DB_USER:postgres.obawaeugxkwkmjvtslip}
spring.datasource.password=${DB_PASSWORD:Priyashri@#}
spring.datasource.driver-class-name=org.postgresql.Driver

# H2 In-Memory Database (Currently Commented Out)
# spring.datasource.url=jdbc:h2:mem:portfoliodb
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=password
# spring.h2.console.enabled=true
# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Email Configuration (Brevo)
# Note: We are using port 2525 instead of 587 because Render Free Tier blocks outbound traffic on port 587.
spring.mail.host=smtp-relay.brevo.com
spring.mail.port=2525
spring.mail.username=b1ea35001@smtp-brevo.com
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Email Settings
# Set the "from" address to your verified Brevo sender email (usually the email you signed up with)
portfolio.email.from=aravindh2003s@gmail.com
portfolio.email.to=aravindh2003s@gmail.com
