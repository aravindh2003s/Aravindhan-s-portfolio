server.port=8080
spring.application.name=Aravindhan Portfolio

# Thymeleaf configuration
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# DevTools
spring.devtools.restart.enabled=true

# PostgreSQL Database Configuration (Using Environment Variables for Production)
spring.datasource.url=${DB_URL:jdbc:postgresql://aws-0-ap-northeast-1.pooler.supabase.com:6543/postgres?prepareThreshold=0}
spring.datasource.username=${DB_USER:postgres.obawaeugxkwkmjvtslip}
spring.datasource.password=${DB_PASSWORD:Priyashri@#}
spring.datasource.driver-class-name=org.postgresql.Driver

# H2 In-Memory Database (Currently Commented Out)
# spring.datasource.url=jdbc:h2:mem:portfoliodb
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=password
# spring.h2.console.enabled=true
# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Email Configuration (Gmail App Password Required)
# IMPORTANT: Gmail requires an App Password, not your regular password!
# Steps to get App Password:
# 1. Enable 2FA: https://myaccount.google.com/security
# 2. Generate App Password: https://myaccount.google.com/apppasswords
# 3. Replace the password below with the 16-character app password

# To DISABLE email (form will still work and save to database):
# Comment out all spring.mail.* properties below or set spring.mail.host to empty

# Email is currently DISABLED - uncomment and configure to enable
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=aravindh2003s@gmail.com
# Replace this with your Gmail App Password (16 characters, no spaces)
spring.mail.password=hbevthpcnkhzxjqw
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Email Settings
portfolio.email.from=aravindh2003s@gmail.com
portfolio.email.to=aravindh2003s@gmail.com
